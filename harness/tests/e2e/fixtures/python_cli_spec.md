# Requirements

Build a minimal Python CLI fixture for harness behavior validation.

# Architecture

The harness should parse phases, run task build, execute, review, cleanup, and evaluate paths.

# Verification

Assertions target harness artifacts and state transitions, not fixture app completeness.

# Test Plan

Use mocked Claude responses for deterministic E2E coverage.

# Build Plan

## Phase 1: Project Setup

Create the smallest project-level setup needed for a CLI fixture.

## Phase 2: CLI Core [python]

Implement one small Python CLI behavior.

