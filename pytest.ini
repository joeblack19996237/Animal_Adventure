[pytest]
pythonpath = . deploy/scripts
norecursedirs = workspace dist .git node_modules .pytest_cache
markers =
    e2e: harness end-to-end tests
    live_e2e: opt-in live Claude CLI smoke tests
    integration: integration tests that require real services
